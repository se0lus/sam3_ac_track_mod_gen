# =============================================================================
# SAM3 Track Seg — Python Dependencies
# =============================================================================
# Environment: Python 3.12, CUDA 12.6, Windows
#
# Setup steps:
#   1. conda create -n sam3 python=3.12
#   2. conda activate sam3
#   3. Install PyTorch with CUDA (see below)
#   4. pip install -r requirements.txt
#   5. pip install -e ./sam3/
#
# PyTorch (install FIRST, with matching CUDA version):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu126
#
# Local packages (NOT on PyPI, use local dirs):
#   pip install -e ./sam3/          # SAM3 model (brings timm, ftfy, iopath, etc.)
#
# Model weights:
#   Place sam3.pt in model/sam3.pt  (copy from existing machine)
# =============================================================================

# --- Core ---
numpy>=1.26.2
Pillow>=10.0
opencv-python>=4.8

# --- Geospatial ---
rasterio>=1.3

# --- Scientific ---
scipy>=1.11
scikit-image>=0.21

# --- AI / LLM ---
google-genai>=1.0

# --- Testing ---
pytest>=7.0
